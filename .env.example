# =============================================================================
# ShortiFy Environment Configuration
# =============================================================================
# Copy this file to .env and fill in actual values.
# The .env file is gitignored and should never be committed.
# =============================================================================

# -----------------------------------------------------------------------------
# SQL Server Configuration
# -----------------------------------------------------------------------------
# Password for the SQL Server SA account
# Must meet SQL Server password complexity requirements:
# - At least 8 characters
# - Contains uppercase, lowercase, numbers, and special characters
SA_PASSWORD=<your-sa-password>

# SQL Server port exposed to host (default: 1433)
SQL_SERVER_PORT=1433

# -----------------------------------------------------------------------------
# Redis Configuration
# -----------------------------------------------------------------------------
# Redis port exposed to host (default: 6379)
REDIS_PORT=6379

# Redis connection string for Docker network
REDIS_CONNECTION=redis:6379

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
# ASP.NET Core environment (Development, Staging, Production)
ASPNETCORE_ENVIRONMENT=Development

# HTTP binding for container
ASPNETCORE_URLS=http://+:8080

# API port exposed to host (default: 5080)
API_PORT=5080

# -----------------------------------------------------------------------------
# OpenTelemetry Configuration
# -----------------------------------------------------------------------------
# Service name for telemetry
OTEL_SERVICE_NAME=ShortiFy

# OTLP endpoint for telemetry export (Docker network address)
OTEL_EXPORTER_OTLP_ENDPOINT=http://aspire-dashboard:18889

# Service version for telemetry
OTEL_SERVICE_VERSION=1.0

# Aspire Dashboard UI port (default: 18888)
ASPIRE_DASHBOARD_PORT=18888

# OTLP gRPC port exposed to host (default: 4317)
OTLP_GRPC_PORT=4317